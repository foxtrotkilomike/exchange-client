import classes from './Dropdown.module.scss';

type DropdownProps = Record<string, string>;

const Dropdown = (props: DropdownProps): JSX.Element => {
  return <div></div>;
};

export default Dropdown;
